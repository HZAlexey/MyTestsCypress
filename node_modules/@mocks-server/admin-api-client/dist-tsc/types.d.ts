/// <reference types="node" />
import type https from "https";
import type { BASE_PATH } from "@mocks-server/admin-api-paths";
export interface AnyObject {
    [key: string]: any;
}
export interface MocksServerConfig {
    [key: string]: any;
}
export interface ApiClientConfig {
    host?: string;
    port?: number;
    https?: boolean;
    agent?: typeof https.Agent;
}
export declare type Id = string;
export declare type CollectionId = Id;
export declare type Protocol = "http" | "https";
export declare type DelayTime = number;
export declare type RouteVariantId = `${Id}:${Id}`;
export declare type ApiPath = string;
export declare type Url = `${Protocol}://${ApiClientConfig["host"]}:${ApiClientConfig["port"]}${typeof BASE_PATH}${ApiPath}`;
export interface CrossFetchOptions extends RequestInit {
    agent?: typeof https.Agent;
}
