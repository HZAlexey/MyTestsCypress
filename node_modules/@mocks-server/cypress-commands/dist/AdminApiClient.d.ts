import type { MocksServerConfig, RouteVariantId } from "@mocks-server/admin-api-client";
import type { MocksServerCypressApiClientConfig } from "./types";
export declare class AdminApiClient {
    private _enabled;
    private _apiClient;
    private _port;
    private _host;
    private _protocol;
    constructor(clientConfig: MocksServerCypressApiClientConfig);
    private _isDisabled;
    updateConfig(mocksServerConfig: MocksServerConfig): Promise<any>;
    useRouteVariant(id: RouteVariantId): Promise<any>;
    restoreRouteVariants(): Promise<any>;
    configClient(customConfig?: MocksServerCypressApiClientConfig): void;
    get url(): string;
}
