/// <reference types="cypress" />

context("Проверка отображения Скриптов разговора", () => {
  it("Проверяем что скрипты есть", () => {
    // выбираем коллекцию моков
    cy.mocksSetCollection("card-tips-attributes-render");

    // переходим на страницу с карточкой
    cy.visit("/card/1");

    //не понимаю почему не отображаются подсказки
  });
});
